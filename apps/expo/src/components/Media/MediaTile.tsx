import { StyleSheet } from "react-native";

interface MediaTileProps {
   url: string;
   isVideo: boolean;
   itemSize: number;
}

export const MediaTile = ({ url, isVideo , itemSize }: MediaTileProps) => {

   return (
      <></>
   )
}

const styles = StyleSheet.create({
   
})